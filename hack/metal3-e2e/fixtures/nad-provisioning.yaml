apiVersion: k8s.cni.cncf.io/v1
kind: NetworkAttachmentDefinition
metadata:
  name: ${NAD_NAME}
  namespace: ${NAD_NS}
spec:
  config: |
    {
      "cniVersion": "0.3.1",
      "name": "${NAD_NAME}",
      "type": "bridge",
      "bridge": "${BR_NAME}",
      "promiscMode": true,
      "ipam": {}
    }
